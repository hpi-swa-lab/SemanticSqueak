documentation
examples

	DateAndTime now ? 'format this as hh:mm'. "'15:27'"
	
	WatchMorph new ? #numberOfChildren. "12"
	WatchMorph new ? #verticalTopMostSubmorph.
	
	Date today ? #daysUntilChristmas. "284"
	
	#(1 2 3 5 6 8 8 10) ? #percentageOfEvenNumbers. "62.5" 
	
	#(14 70 55 13 9 81 23 42 60 60) ? '90th percentile'. "60"
	
	World ? 'all open window labels'.
	
	World ? 'summary of ALL open workspace contents (use `window allStringsAfter: nil` for each window)' .
	
	SemanticStreamingMessage ? 'class responsibilities'.
	
	BlockNode ? 'which design patterns'.
	
	"ThisContext ? 'where is failBlock from and what does it do'."
	
	WatchMorph new ? #makeListOfAllColorsInThisMorph.
	
	World ? 'largest submorph by area'.
	World ? 'smallest submorph by area'.
	
	"aMorph ! 'make this morph blue'."